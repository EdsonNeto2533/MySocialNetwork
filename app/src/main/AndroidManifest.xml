<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mysocialnetwork">

    <application
        android:name=".generics.ui.AppStart"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DrawerLayout">
        <activity
            android:name=".featureDashboard.ui.DashboardActivity"
            android:exported="true"
            android:theme="@style/Theme.DrawerLayout.noActionBar"
            />
        <activity
            android:name=".featureLogin.ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>